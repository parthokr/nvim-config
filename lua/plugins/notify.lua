return {
	"rcarriga/nvim-notify",
	-- config = function()
	--   vim.notify = require("notify")
	--   -- auto hide after 500ms
	--   vim.notify.setup({
	--     stages = "fade_in_slide_out",
	--     -- timeout = 500,
	--   })
	-- end,
}
